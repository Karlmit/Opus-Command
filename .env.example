# Copy this file to .env and fill in your values
# .env is gitignored — never commit the real file

# Required: change this to a random string (run: openssl rand -hex 32)
SESSION_SECRET=change-me-to-a-random-string
